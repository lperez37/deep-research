services:
  deep-research:
    build: .
    env_file: .env
    environment:
      TRANSPORT: http
      PORT: "8087"
    ports:
      - "8087:8087"
    volumes:
      - credit-data:/data
    restart: unless-stopped

volumes:
  credit-data:
