services:
  deep-research:
    build: .
    env_file: .env
    environment:
      TRANSPORT: http
      PORT: "8087"
    ports:
      - "${BIND_HOST:-127.0.0.1}:8087:8087"
    volumes:
      - credit-data:/data
    restart: unless-stopped

volumes:
  credit-data:
